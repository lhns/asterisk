; res_xmpp declines to load with no connection configured. The component below points at
; a port nothing listens on, on purpose: this asserts the module loads, not that it connects.
[general]
autoregister=yes

[asterisk]
type=component
serverhost=127.0.0.1
port=5347
username=asterisk.example.invalid
secret=not-a-real-secret
usetls=no
context=xmpp
